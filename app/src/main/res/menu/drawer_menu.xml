<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:checkableBehavior="single"
        >

        <item
            android:id="@id/dashboard"
            app:actionLayout="@layout/dashboard_menu_item"
            app:showAsAction="always"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@id/vehicleRecord"
            app:actionLayout="@layout/vehicle_record_menu_item"
            app:showAsAction="always"
            tools:ignore="MenuTitle" />

        <item
            android:id="@id/driverSetUp"
            app:actionLayout="@layout/driver_set_up_menu_item"
            app:showAsAction="always"
            tools:ignore="MenuTitle" />

        <item
            android:id="@id/vehicleSetUp"
            app:actionLayout="@layout/vehicle_set_up_menu_item"
            app:showAsAction="always"
            tools:ignore="MenuTitle" />

    </group>
</menu>